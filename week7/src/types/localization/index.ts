/**
 * Represents the localization settings stored in global state or AsyncStorage
 * Global state veya AsyncStorage içinde saklanan lokalizasyon ayarlarını temsil eder
 */
export type LocalizationState = {
  lang: string; // Current language code, e.g., 'en' or 'tr' / Geçerli dil kodu, örn: 'en' veya 'tr'
};
